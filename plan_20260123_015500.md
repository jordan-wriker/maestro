- [ ] **Task 1: Initialize App Structure (@Codex)**
    - **Context:** `mcp-server/app/__init__.py`
    - **Goal:** Create the basic package structure for the refactored application to support the proper separation of concerns.
    - **Steps:**
        - Create `mcp-server/app/__init__.py`.
        - Create `mcp-server/app/api/__init__.py`.
        - Create `mcp-server/app/services/__init__.py`.
        - Create `mcp-server/app/core/__init__.py`.

- [ ] **Task 2: Implement Logging Configuration (@Codex)**
    - **Context:** `mcp-server/app/core/logging_config.py`
    - **Goal:** Configure structured logging using `structlog` to replace standard print/logging for better observability.
    - **Steps:**
        - Create `configure_logging()` function.
        - Set up `structlog` processors for JSON output and console rendering during dev.

- [ ] **Task 3: Implement AppState Class (@Claude)**
    - **Context:** `mcp-server/app/core/state.py`
    - **Goal:** Encapsulate global state (call history, batches) to allow for better testing, thread safety, and future database migration.
    - **Steps:**
        - Create `AppState` class.
        - Move `call_history` and `batches` global variables structure into this class.
        - Implement methods for safely adding to `call_history` with thread locking if necessary.

- [ ] **Task 4: Move Parsing Logic (@Codex)**
    - **Context:** `mcp-server/app/services/parsers.py`
    - **Goal:** Extract text parsing logic from the god-object `server.py` into pure functions to improve testability.
    - **Steps:**
        - Move `extract_xml` function from `server.py`.
        - Move `parse_claude_events` function from `server.py`.
        - Ensure they are stateless pure functions.

- [ ] **Task 5: Implement AgentRunner Services (@Claude)**
    - **Context:** `mcp-server/app/services/agents.py`
    - **Goal:** Create a service to handle agent execution using AsyncIO, replacing blocking subprocess calls to fix server blocking issues.
    - **Steps:**
        - Define `AgentRunner` abstract base class/protocol.
        - Implement `AsyncSubprocessRunner` using `asyncio.create_subprocess_exec`.
        - Ensure it captures stdout/stderr properly and uses the new logging system.

- [ ] **Task 6: Implement BatchManager (@Claude)**
    - **Context:** `mcp-server/app/services/batch.py`
    - **Goal:** Manage batch processing logic, utilizing the `AgentRunner` and `AppState`, separating it from the HTTP layer.
    - **Steps:**
        - Create `BatchManager` class.
        - Implement methods to submit batches and poll status.
        - Integrate with `AppState` for persistence (in-memory for now).

- [ ] **Task 7: Re-implement API Routes (@Codex)**
    - **Context:** `mcp-server/app/api/endpoints.py`
    - **Goal:** Re-create the FastAPI routes using the new services and state management to expose the functionality.
    - **Steps:**
        - Define FastAPI router.
        - Implement `/call` endpoint using `AgentRunner` and `AppState`.
        - Implement `/batch` related endpoints using `BatchManager`.

- [ ] **Task 8: Create Main Entrypoint (@Codex)**
    - **Context:** `mcp-server/app/main.py`
    - **Goal:** Assemble the application, initializing state and routers, replacing the old `server.py` entry point.
    - **Steps:**
        - Initialize `FastAPI` app.
        - Call `configure_logging()`.
        - Initialize global `AppState`.
        - Include API routers from `app/api/endpoints.py`.

- [ ] **Task 9: Update Dependencies (@Codex)**
    - **Context:** `mcp-server/requirements.txt`
    - **Goal:** Add new dependencies required for the refactor to ensure the environment is correct.
    - **Steps:**
        - Add `structlog`.
        - Add `tenacity`.
        - Ensure `fastapi`, `uvicorn` are present.

- [ ] **Task 10: Verify Race Conditions (@Claude)**
    - **Context:** `mcp-server/tests/test_concurrency.py`
    - **Goal:** Create a test to verify that the new implementation handles concurrent requests without data corruption (addressing Critical Issue #1).
    - **Steps:**
        - Write a test script using `pytest` and `asyncio` or `httpx`.
        - Fire multiple concurrent requests to the new app.
        - Assert that `AppState` remains consistent and no data is lost/overwritten.
